using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemySpawner : MonoBehaviour
{
    private enum EnemyType { Enemy1, Enemy2, Enemy3 }
    private enum EnemyFireType { Radian, Direct,  }
    private enum BehaviourType { S_type, Z_type, }
}
